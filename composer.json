{
    "name": "maga/library-management-system",
    "description": "Library Management System — books, members, librarians, borrowings and returns.",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "ext-pdo": "*"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}
